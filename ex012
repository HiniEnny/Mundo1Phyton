#Faça um algoritmo que leia o preço de um produto e mostre seu novo valor, com 5% de desconto.
p = float(input('Digite o preço do produto:R$'))
d = p - ((p*5)/100)
print('O valor era R${:.2f}, com o desconto de 5% ficou R${:.2f}'.format(p, d))
